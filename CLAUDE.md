# Project instructions

Read and follow [AGENTS.md](AGENTS.md), the shared project rules for Codex and Claude.

When using this Skill from Claude, the workbench still calls the authenticated Codex image provider. Do not change provider or duplicate the shared rules based on the host agent.
